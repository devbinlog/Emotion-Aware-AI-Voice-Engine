# Next.js가 백엔드를 프록시하므로 (next.config.mjs의 rewrites) 기본적으로 비워둬도 됩니다.
# 백엔드가 다른 호스트/포트에 있는 경우만 설정하세요.

# REST API base URL (기본값: 빈 문자열 = 상대경로 /api/* 사용)
NEXT_PUBLIC_API_URL=

# WebSocket URL (기본값: ws://localhost:8000)
NEXT_PUBLIC_WS_URL=ws://localhost:8000
