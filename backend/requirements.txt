# ── Web Framework ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Audio I/O ──────────────────────────────────────────────────────────────
numpy>=1.24.0
soundfile>=0.12.1
librosa>=0.10.1

# ── VAD ───────────────────────────────────────────────────────────────────
# silero-vad is loaded via torch.hub — PyTorch is the only hard dependency.
torch>=2.0.0
torchaudio>=2.0.0

# ── STT ───────────────────────────────────────────────────────────────────
faster-whisper>=1.0.0

# ── TTS (MVP: Coqui VITS — CPU-safe, pure pip install) ─────────────────────
# See reports/model_choices.md for engine comparison.
TTS>=0.22.0

# ── Optional: Piper TTS (ONNX, fastest CPU engine, limited Korean) ─────────
# Uncomment if switching TTS_ENGINE=piper
# piper-tts>=1.2.0

# ── Optional: ONNX runtime (for future model optimizations) ────────────────
# onnxruntime>=1.17.0
