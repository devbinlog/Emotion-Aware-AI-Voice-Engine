# Copy to .env and adjust as needed.

# TTS engine: coqui (default, CPU-safe) | piper | xtts
TTS_ENGINE=coqui

# Whisper model size: tiny | base | small | medium
WHISPER_MODEL_SIZE=small

# Whisper language (ISO-639-1). Leave blank for auto-detection.
WHISPER_LANGUAGE=ko

# VAD speech threshold (0–1)
VAD_THRESHOLD=0.5

# Emotion fusion weights (must sum to 1.0)
EMOTION_AUDIO_WEIGHT=0.6
EMOTION_TEXT_WEIGHT=0.4

# Piper model path (only needed if TTS_ENGINE=piper)
TTS_PIPER_MODEL=models/tts/en_US-lessac-medium.onnx

# Log level: DEBUG | INFO | WARNING
LOG_LEVEL=INFO
